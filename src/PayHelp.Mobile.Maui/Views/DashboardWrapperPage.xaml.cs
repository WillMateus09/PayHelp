using Microsoft.Maui.Controls;

namespace PayHelp.Mobile.Maui.Views;

// Página wrapper obsoleta - mantida apenas para evitar referências quebradas.
// Não utilizada mais. Conteúdo removido para impedir erros de compilação.
public partial class DashboardWrapperPage : ContentPage
{
    public DashboardWrapperPage()
    {
        InitializeComponent();
        // Intencionalmente vazio.
    }
}
